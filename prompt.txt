I'd like to build a full stack app that uses a Rust-powered backend alongside a React page frontend to help aid learning, particularly based on language learning via N+1 methodology. This may show additional capabilities at drilling-related learning (rote learning for K-12 curricula, general knowledge expansion). This is underpinned by the simple concept that is N+1: information is provided via "comprehensible input", where most of a sentence or concept is accepted to be fully understood by the user. As such, the following should be present in the repo:

1. A way to establish what the user already understands: for now, we can target existing Anki deck contents via the AnkiConnect API. This will help us to establish a baseline level that the user currently sits at for language acquisition. We can later bulk this out with integrated eBook tracking, scraped video/podcast subtitles and transcripts. Scaffold the basic architecture for tracking this additional information as the user interacts with sites: our only live interaction for now will be via URLs provided by the user explicitly on the frontend (see step 4). I'd also like to see "testing in" implemented on the frontend: these should test current user understanding of targeted topics for creating a simple "baseline" database of understanding when beginning a new subject, especially when Anki information is not available/applicable. This should also include a "tags" section that allows us to understand the user's interests, so we can provide comprehensible input that the user has genuine interest in.
2. A way to process this information: We'll use a freshness database to track user comprehension and update it when the user interacts with media including the targeted word or concept. For instance, if the user is taking a quiz on a webpage or interacts with package-provided flashcard containing L'Hopital's Rule, we can update the held "freshness" of the concept to reflect updated familiarity tracking, as well as upstream concepts like limits. For language learning, we may establish a variety of pages that fit different vocabulary strengths, and provide content to language learners that contains 90% comprehensible words alongside 10% unknown/low-comprehension words. Build out a small mock database for each topic that provides a small vector of input documents / tests / YouTube videos of gradually increasing complexity. You can manually grade these for now, but these complexities will later need AI API interaction for live grading / evaluation as well as establishing important "tags" -- you are expected to build this out.
3. A way to provide new comprehensible input: We'll need to establish a database of information via scraped legally available blogs, Youtube caption extraction endpoints, etc. that present usage of these concepts/words alongside additional challenging content. This should be built via Playwright content scraping and YouTube's official caption API interaction tool. For example, if a user is learning a language, we can provide flashcards that contain their current vocabulary and provide sentences with additional "target" words, as well as real-world links to content including similar sentences that obey the true N+1 rule. We should be using both input data stored locally in our database as well as live search to be able to serve input data that is A. relevant to the users interest B. relevant to the user's comprehension, containing an overall amount of comprehended words that falls within the comprehensive input threshold and C. 
4. A way to view statistics and analytics about learning targets and pace: This is the role of the frontend -- providing insights into user learning, new words/concepts, and usage graphs. Eventually, we can use this to track out "courses", typical learning routes that reflect standard curricula. This frontend should also bundle some flashcards, serve them for the user, and track them for freshness updates. We should also build out some direct Anki interconnection here, for live updating information currently held on Anki. This should be built in React through Vite with Tailwind CSS styling for consistent component structure. All data should be grounded via live syncing against package endpoint and Anki deck. Testing for this package should involved exhaustive edge-case handling and component mock tests, and interaction pipelines should fail gracefully for clean UX. We can use our AI API endpoint for creating simple "comprehension tests" served on the front end as well. These can be simple multiple choice tests that target concepts at the frontier of their understanding to help drill in important concepts. User should be provided with a GitHub style usage graph, deck creation tool, and other graphs that show knowledge growth rate. Flashcards should be presented as components with a "Flipping" animation.
5. A flywheel to improve the product systemically: We'll need a way to capture telemetry from usage data, learning rates, course refinement, as well as capturing faulty or problematic data that doesn't represent positive learning outcomes. We'll need to agglutinate information from a variety of end users into one large database, derive meaningful statistics about individual users and training material, capture important metrics like downtime and user tickets, and more. To do this, we'll need to apply a weight to input data for our use case that will help us refine the product by serving more effective input to end users -- effective learning material should be rewarded and served more often based off of composite success metrics from our data capture. Telemetry information should be captured via Grafana and Sentry integration, as well as PostHog usage for fine-tuning gradations like the aforementioned 90:10 comprehensiveness ratio.

This is a SaaS buildout for education software, so we'll need a couple of builds that we can work from to show scalability and functionality. All base functionality should be intact and fully unit-tested before finishing conversation. We'll also need the basic buildout with respects to AWS, how we'll likely be hosting this, for user management and master database bucketing. Build these out, but mock these systems locally for E2E testing flows for now -- I'm expecting a functional college-level Physics 201 and Japanese Language 103 path for now. Current database structure should reflect the general flow of the overall course and be marked by a general "fittedness" to the current comprehension of the user -- provide a mix of focused and general content that provide a decent outline of all topics targeted in the course, and benchmark data retrieval and storage times so we can track as we build out expansions. Build the backend to work in a local Docker container for now, as we'll be hosting it on the cloud in the future. Have the container expose the frontend for direct interaction and leave running after task conclusion. Use FSRS algorithm for freshness tracking, AI-powered API endpoints for information parsing, and Postgres for our persistent store for now. All external usages (AI API, AWS usages) should be built out in the live repo but replaced by mocks for E2E and unit testing. Ensure all tests are passing, relevant information is correctly tracked, successful E2E runs, and clean final output UI before concluding turn. 

Create a few profiles that have different levels of comprehension for the initial check, and these profiles should be switchable on the frontend to show the robust implementation of the lesson system. Additionally, include some mocked logs for Grafana and Sentry, as well as sample PostHog analytics. This is a large task, but work autonomously through entire spec. Disregard concerns about time/token usage.